# Setup instructions

1. unzip the project folder
2. cd into the directory
3. run `npm install` to install dependencies
4. run `npm run dev:android` to compile and launch the app in an Android emulator
   or connected device.  You can also run `npm run dev:ios` to launch in an iOS
   simulator or device.